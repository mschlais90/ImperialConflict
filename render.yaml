services:
  - type: web
    name: imperial-conflict
    runtime: node
    plan: free
    rootDir: phaser
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production
